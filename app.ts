export { };

const a: number = 10;
const b: number = 20;

const myName: string = "Le Cong Dinh";
const myAge: number = 20;
const myStatus: boolean = true;
const myObj: { id: number, name: string } = { id: 1, name: "Dinh" };
const arrNumber: number[] = [1, 2, 3, 4];
const arrObj: { id: number, name: string }[] = [{ id: 1, name: "San pham A" }, { id: 2, name: "San pham B" }]

function sum(numA: number, numB: number) {
    return numA + numB;
}
console.log(10, 20);

