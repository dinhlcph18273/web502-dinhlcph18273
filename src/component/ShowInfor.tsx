import React from "react";

type ShowInforProps = {
}

const ShowInfor = (props: ShowInforProps) => {
    console.log(props);
    return (
        <h1>ShowInfor</h1>
    )
}

export default ShowInfor;